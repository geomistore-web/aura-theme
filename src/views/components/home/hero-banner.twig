{#
| Variable                       | Type     | Description                                                                  |
|--------------------------------|----------|-------------------------------------------------------------------------------|
| component                      | object   | Merchant-filled fields from twilight.json `home.hero-banner` component        |
| component.slides[].image       | string   |                                                                               |
| component.slides[].eyebrow     | ?string  |                                                                               |
| component.slides[].title       | string   |                                                                               |
| component.slides[].description | ?string  |                                                                               |
| component.slides[].cta_text    | ?string  |                                                                               |
| component.slides[].url         | ?string  |                                                                               |
| position                       | int      | Sorting number start from zero                                               |
#}
<section component-id="{{ componentId }}" data-testid="store-home-hero" class="aura-hero">
    <salla-slider id="aura-hero-slider-{{ position }}" auto-play static-when-single
                  {{ component.slides|length > 1 ? '' : 'show-controls="false"' }} type="fullwidth">
        <div slot="items">
            {% for slide in component.slides %}
                <div class="swiper-slide aura-hero__slide">
                    <div class="aura-hero__bg" style="background-image:url({{ slide.image }});"></div>
                    <div class="aura-hero__scrim"></div>
                    <div class="container aura-hero__inner">
                        <div class="aura-hero__content">
                            {% if slide.eyebrow %}<span class="aura-eyebrow">{{ slide.eyebrow }}</span>{% endif %}
                            <h2 class="aura-hero__title" data-swiper-parallax="-400">{{ slide.title }}</h2>
                            {% if slide.description %}<p class="aura-hero__desc" data-swiper-parallax="-250">{{ slide.description }}</p>{% endif %}
                            {% if slide.cta_text %}
                                <a href="{{ slide.url ?: '#' }}" class="btn-aura-gold">{{ slide.cta_text }}</a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </salla-slider>
</section>
